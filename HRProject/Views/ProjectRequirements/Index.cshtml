@* @model IEnumerable<HRProject.Models.ProjectRequirement>

@{
    ViewData["Title"] = "Project Requirements";
}

<h2 class="mb-3">Project Requirements</h2>

@if (Model != null && Model.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Project</th>
                <th>Competence</th>
                <th>Min Level</th>
                <th>Min Years</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.Project?.Name</td>
                    <td>@r.Competence?.Name</td>
                    <td>@r.MinLevel</td>
                    <td>@r.MinYearsOfExperience</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No requirements defined yet.</p>
}
 *@